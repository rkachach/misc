;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "/home/rkachach/.emacs.d/lisp/emacs-for-python-master/extensions/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 0)
        (left . 0)
        (width . 140)
        (height . 53)))

